#include <stdio.h>
#include <stdlib.h>
#include <fnctl.h>

int main() {
  int fd = open( img.ppm, O_CREAT | O_TRUNC | O_WRONLY, 0644);
  char * img;
  sprintf(img, %s, "P3\n500 500 255");
  write(fd, img, strlen(line));
  int i = 0;
  for (i = 0; i < 500, i++) {
    char r;
    char g;
    char b;

  }
}
