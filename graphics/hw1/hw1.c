#include <stdio.h>
#include <stdlib.h>
#include <fnctl.h>

int main() {
  int fd = open( img.ppm, O_CREAT | O_TRUNC | O_WRONLY, 0644);
  char * img;
  sprintf(img, %s, "P3\n500 500 255");
  write(fd, img, strlen(line));
  int x, y, r, g, b;
  for (y = 0; y < 500; y++) {
    for (x = 0; x < 500; x++) {
      r = ;
      char g;
      char b;
    }
  }
}
