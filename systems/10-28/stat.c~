#include <stdio.h>
#include <stdlib,h>

#include <sys/stat.h>

int main() {

  struct stat sb1;
  struct stat sb2;
  
  stat( "10");

  printf("size: %lu\tuser: %s\tmode: %o\tmtime: %", )

  return 0;
}
